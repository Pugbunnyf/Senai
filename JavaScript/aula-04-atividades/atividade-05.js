const pessoa = {
  nome: "Caetano",
  idade: 15,
  altura: 1.83,
  estudante: true
};

const frase = `Meu nome é ${pessoa.nome}, tenho ${pessoa.idade} anos, ${pessoa.altura}m de altura e sou estudante no Cemajoba: ${pessoa.estudante}.`;

console.log(frase);